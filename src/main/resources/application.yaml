spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/your_qr_db
    username: postgres
    password: x10x23057
  mvc:
    hiddenmethod:
      filter:
        enabled: true

